{
  "name": "spatial-partition",
  "version": "1.0.0",
  "description": "2D Spatial Partitioning",
  "main": "index.js",
  "scripts": {
    "test": "browserify --standalone SpatialPartition src/spatialPartition.js -o dist/spatialPartition.js && mocha",
    "build": "npm run test && uglifyjs dist/spatialPartition.js > dist/spatialPartition.min.js"
  },
  "author": "Adrian Hecker",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^14.0.0",
    "mocha": "^3.2.0",
    "uglify-js": "^2.7.5"
  }
}
