'use strict';

function spatialPartition(width, height, cellWidth, cellHeight) {

}

spatialPartition.prototype.add = function(entity) {
    // add a single entity
}

spatialPartition.prototype.addAll = function(entities) {
    // add all entities
}

spatialPartition.prototype.move = function(entity, x, y) {
    // move an entity
}

spatialPartition.prototype.x = function() {
    // set the x-dimension accessor
}

spatialPartition.prototype.y = function() {
    // set the y-dimension getter
}

module.exports = spatialPartition;